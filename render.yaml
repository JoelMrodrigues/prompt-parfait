# Render - Déploiement API Backend
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: prompt-parfait-api
    runtime: node
    region: frankfurt
    rootDir: server
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: RIOT_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
